# LLM — local Qwen via MLX-LM on Apple Silicon
LLM_PROVIDER=mlx
MLX_BASE_URL=http://host.docker.internal:8080
MLX_MODEL=mlx-community/Qwen2.5-7B-Instruct-4bit

# Infrastructure — use Docker service names for inter-container networking
POSTGRES_HOST=postgres
CHROMA_HOST=chromadb
CHROMA_PORT=8000
NEO4J_URI=bolt://neo4j:7687
REDIS_URL=redis://redis:6379/0

# GitHub Integration
# Personal access token (classic) with repo scope, or a GitHub App installation token
GITHUB_TOKEN=
# Webhook secret — set the same value in your GitHub repo's webhook settings
GITHUB_WEBHOOK_SECRET=changeme-webhook-secret
# API key used to authenticate the GitHub Action → Helix webhook call
HELIX_API_KEY=changeme-generate-a-real-key